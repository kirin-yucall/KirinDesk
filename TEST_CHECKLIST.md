# KirinDesk 实机测试清单（志愿者版）

> 👋 **你好！** 这是一份面向社区志愿者的实机测试清单。KirinDesk 是个人开发者维护的纯公益项目，开发者的设备有限（只有一台 Windows、且本机 GPU 驱动有损坏），很多功能需要不同的硬件 / 操作系统 / 网络环境才能真正验证。**如果你正好有相应的设备，欢迎按这份清单跑一跑，把结果反馈给我们——这是对项目最大的支持。**

---

## ❓ 怎么用这份清单

### 1. 准备环境

| 角色 | 要求 |
|------|------|
| **主控端**（发起连接的一方） | Windows 10/11，装好 KirinDesk。有 4K 屏更好（能覆盖 4K 编码路径） |
| **被控端**（被远程的那一方） | Windows 10/11，装好 KirinDesk |
| **第二台被控**（可选） | 测试多窗口隔离时需要——Windows 或 Linux（Ubuntu）都行 |
| **网络** | 两台机器能互通（同局域网 / 公网 IPv4 / IPv6 都欢迎，不同网络环境都能帮我们发现问题） |
| **日志** | 出问题时，主控端和被控端各自的日志都很有用。GUI 可以在「日志」页看，命令行模式直接看控制台输出 |

> 💡 **想测 Linux / macOS？** 太好了！目前这两个平台的代码已经写好但**几乎没人实机验证过**，特别需要 Linux（Ubuntu）和 macOS 用户帮忙跑（见清单最后的「平台延伸」组）。Windows 用户只需关注 A~I 组。

### 2. 跑测试

- 看每组的「操作」列照做，「预期」列是你**应该看到的结果**。
- 看到预期结果 → 在「结果」列打个 ✅，简单写几句也行（比如「正常」「1080p 流畅，延迟约 30ms」）。
- 和预期不符 → 打个 ❌，**尽可能详细记录**：现象、你的环境（系统版本 / 显卡 / 网络）、复现步骤。截图或录屏非常有帮助。
- 跑不动 / 闪退 / 卡住 → 同样记 ❌，把错误日志贴上。

### 3. 反馈结果（任选其一）

| 方式 | 适合 |
|------|------|
| **开一个 GitHub Issue**（推荐） | 最规范。标题写 `[实机测试] A组画面音频键鼠 - 你的环境`，把清单对应的几行结果贴进去，附截图/日志 |
| 在 Issue 评论区直接回复 | 如果你只是顺手跑了几个项目，简短反馈也很好 |
| 提交 PR 补充这份清单 | 如果你想长期贡献 |

> 🙏 **哪怕你只跑了其中一个组、甚至只跑了几个项目，都是有价值的反馈。** 不必跑完全部才能汇报——每一台真实设备的验证都在帮项目前进。

---

## A 组 · 远程桌面对决（核心功能，最重要）

> 这一组是远程控制最核心的体验，也是当前最缺实机验证的部分。**如果你只能跑一组，请跑这一组。** 需要两台 Windows 互通。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| A01 | 远程画面 | 两台机器互连（被控端最好有 4K 屏） | 主控端的连接窗口里出现**实时画面**；被控端日志持续显示画面帧编码正常，没有 `Capture send error` 之类的报错 | ☐ |
| A02 | 双向音频 | ①被控端打开「允许麦克风」→ 主控端能听到被控端的声音；②主控端连接窗口里打开「麦克风」→ 被控端能听到主控端的声音 | 两个方向都能出声；被控端日志显示 opus 音频编码器正常打开，**没有** `-22` 之类的报错 | ☐ |
| A03 | 键鼠控制 | 在主控端操作鼠标（移动 / 点击 / 拖拽）、键盘输入 | 被控端响应正常，没有明显滞后或堆积。**特别留意小动作（细微移动、快速点击）的跟手程度**——这是这次重点优化的地方 | ☐ |
| A04 | 文件传输 | 双向各传一个文件（小文件 + 大文件都试） | 传输完成、文件完整无损；主控端还可以跑 `kirin_desk --cli self-test`（里面自带文件往返测试）确认 | ☐ |
| A05 | 多显示器切换 | 被控端接两台或更多显示器，主控端切换查看 | 切换正常、画面不错位、坐标对得上 | ☐ |

---

## B 组 · 多窗口与会话管理

> 测试同时连多台机器、开多个会话的稳定性。需要至少一台主控 + 一台被控，部分项目要两台被控。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| B01 | 同一设备不重复开窗 | 对同一台被控连两次（两次都用 IP，或 IP 和域名各一次） | 只弹一个窗口；第二次点击只是让已有窗口「浮上来」，不会开第二个会话；日志里不会出现第二条会话 | ☐ |
| B02 | 关窗后能重连 | 关掉连接窗口，再点一次连接 | 能正常新建窗口、正常出画面 | ☐ |
| B03 | 多窗口互不串台 | 同时连两台**不同的**被控，观察 10 秒 | 两个窗口各自显示各自的画面；FPS / 分辨率统计各算各的，不会跳来跳去 | ☐ |
| B04 | Shell 终端 | 开一个 Shell 窗口，执行 `ls`、`echo hello` | 远端输出能正常看到、内容正确 | ☐ |
| B05 | 单连接体验不退化 | 只连一台机器，观察画面 / 统计 / 输入 | 和你印象里的旧版本一样，没有变差 | ☐ |
| B06 | 反复开关不泄漏线程 | 打开→关闭连接窗口重复 5 次（桌面 + Shell 混着来） | 线程数会回落到基线，不会一直涨上去（可用任务管理器观察进程的线程数） | ☐ |
| B07 | 中文显示 | Shell 里执行 `echo 中文测试`、`ls` 看中文文件名、用 `vim` 打开中文文件 | 中文对齐正确、没有乱码或「豆腐块」□ | ☐ |
| B08 | 回看完整 | 在 Shell 里输出 5000 行（含中文），然后往上滚动回看 | 中文完整、不丢字符 | ☐ |
| B09 | 中文输入法 | 用微软拼音输入「中文测试」 | 打字时组合期有就地预览（反显或下划线）；按空格提交后回显正确 | ☐ |
| B10 | 中文全链路 | `cat` 中文内容回显、粘贴中文、组合期按 Backspace / 方向键 | 全程 UTF-8 不乱码 | ☐ |
| B11 | 断线覆盖层 | 桌面连接时，把被控端进程杀掉 | 主控端窗口显示「连接已断开 / Connection lost」的覆盖层 | ☐ |
| B12 | 断开后服务端干净 | 主控端断开 Shell 后，去被控端看 | 会话任务退出、没有残留的 PTY 进程 | ☐ |

---

## C 组 · Dashboard 仪表盘与服务端设置

> 主要看主界面的各种开关、设置、显示是否正确。**单机就能测大部分**。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| C01 | 设备 ID 自动生成 | 在配置里清空设备 ID 后重启 | 日志出现身份恢复的警告；会生成新的身份；**不会**出现 `No device identity loaded` | ☐ |
| C02 | 设备 ID 稳定 | 在 Dashboard 身份卡看 Device ID | 显示类似 `HD-XXXXXXXX` 的格式；保存配置后重启，ID 保持不变 | ☐ |
| C03 | 新身份能握手 | 用新身份从主控端连一次 | 主控端会提示是否信任新指纹，确认后能握手成功 | ☐ |
| C04 | IPv4 连接 | 被控端监听后，主控端用 `127.0.0.1:端口` 或局域网 IPv4 连接 | 能连上；同时 IPv6 客户端不受影响 | ☐ |
| C05 | Connect 页填 IPv4 | IP 模式下输入 `192.168.x.x:端口` | 校验通过、能完成连接（旧版本会报「不是有效的 IPv6 地址」） | ☐ |
| C06 | 身份卡显示 IPv4 | 看 Dashboard 身份卡 | 有 IPv4 那一行；没有 IPv4 时显示 N/A | ☐ |
| C07 | 允许受控开关 | 打开 / 关闭「允许受控」开关 | 开 → 状态显示「监听中 :端口」；关 → 停止监听；如果端口被占用导致启动失败，开关会自动回到关，并显示失败原因 | ☐ |
| C08 | IP / 域名 模式切换 | 点工作模式按钮互换 | Settings / Connect / Dashboard 三页同步；保存重启后保持 | ☐ |
| C09 | 临时连接自动过期 | 打开「临时连接」开关，等窗口时间过去 | 开关自动回到关；日志有 `expired`（过期）记录；之后未知设备会被白名单拒绝 | ☐ |
| C10 | 改服务端设置 | 在 Dashboard 改端口 / 昵称 / 挑战码 → 保存 → 重启服务端 | 新端口生效；用新昵称 / 挑战码能握手成功 | ☐ |
| C11 | 两个开关同一行 | 看 Dashboard 的服务端卡 | 「允许受控」和「允许麦克风」两个开关在同一行；停止时没有「已停止」文字；启动失败会显示原因 | ☐ |
| C12 | 麦克风默认关 | 新开一个进程看开关 | 「允许麦克风」默认是关；即使把总开关开了→关→开循环，这个子开关仍然保持关 | ☐ |
| C13 | 整页可滚动 + 身份卡 | 缩小窗口；看身份卡 | 整页能上下滚动；身份卡只有 5 行：Device ID / IPv6 / IPv4 / Domain / Listen Port | ☐ |
| C14 | Settings 没有 Server 组 | 打开 Settings 页 | **没有** Server 折叠组（已搬到 Dashboard）；音频总开关在 Dashboard；高危警告（旁路 + 空挑战码）在 Dashboard 显示 | ☐ |
| C15 | 无人值守三滑块 | Settings → Unattended | 三个横向滑块；保存重启后保持；开机自启的注册 / 卸载和状态徽标同步 | ☐ |
| C16 | 白名单免审批 | 用命令行 `whitelist add` 加通配 / 过期条目后，GUI 模式连接 | 白名单命中的设备**免审批**直接进入；完全没见过的设备还是弹审批；无人值守模式下未知设备直接拒绝 | ☐ |
| C17 | DNS 服务商列表 | Settings → DNS | 组名是「DNS」+ 服务商下拉框（共 20 家可选）；选一家后动态显示对应的凭据字段；保存重启保持 | ☐ |
| C18 | 公网检测 | 看 Dashboard 身份卡 | IPv6 / IPv4 每个地址后面有红 / 绿点（绿=公网，红=内网）；探测中显示「公网检测中…」；如果都不是公网，会提示「无公网地址建议开启内网穿透或端口转发」 | ☐ |
| C19 | Connect 双栏 + 设备列表 | 看 Connect 页 | 输入框右边是连接日志；下面是「连接过的设备」列表，单击填入、右键可连接 / 编辑 / 删除；空列表显示「暂无记录」 | ☐ |
| C20 | 更新检查提示 | Settings 里点检查更新（仓库还没发 Release 时） | 提示「暂无 Release 发布（HTTP 404）— 请在 GitHub 发布 Release 后重试」 | ☐ |
| C21 | Tunnel 页 | 导航到 Tunnel 页（🚇） | 有「▶ 启动 / ■ 停止」按钮和 Client / Server 模式区块（详见 I 组） | ☐ |

---

## D 组 · Devices 设备列表

> 测试设备卡片的管理和排序。单机可测。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| D01 | 卡片信息 | 看 Devices 页的设备卡 | 显示昵称 + 备注名（备注为空时显示「—」） | ☐ |
| D02 | 排序与持久化 | 连接 / 编辑 / 删除 / 上移 / 下移各操作一轮，然后重启程序 | 顺序能交换、重启后保持 | ☐ |
| D03 | 编辑弹窗 | 编辑备注名 / (IP或域名):端口 / 昵称 / 挑战码 | 四个字段都能改；空昵称可保存；端口非法时**禁用保存**按钮 | ☐ |
| D04 | 挑战码预填 | 设备的挑战码非空时，单击这个设备 | Connect 表单的挑战码会自动填好 | ☐ |
| D05 | 新设备加末尾 | 连接一个全新的设备并成功 | 自动保存到列表末尾，不打乱已有的手动排序 | ☐ |
| D06 | 旧数据兼容 | 用旧版本的 devices.json | 不报错，新字段用默认值，排序按上次在线时间降序 | ☐ |
| D07 | 三开关联动 | 全新配置下试：无人值守 开 / 关；默认受控 开 → 看 Dashboard 允许受控；关掉允许受控 → 默认受控保持；默认受控 开 + 无人值守 关 → 重启 | 默认全关；联动和独立翻转符合状态机；重启后默认受控仍自动监听；自启注册 / 注销一致 | ☐ |

---

## E 组 · 语言与界面文案

> 测试中英文切换和界面文字。单机可测。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| E01 | Connect 页不再有进度条 | 点 Connect 发起连接 | Connect 页**不再出现**顶部状态点和步骤条；进度只看右边的连接日志；连接时按钮仍然禁用 / 转圈 | ☐ |
| E02 | 会话窗口状态条 | 打开桌面会话窗口 | 在画面之前有一条状态条：绿点 + 「Connected to …@… (transport: TCP)」+ 步骤条停在第 3 步；颜色正确 | ☐ |
| E03 | Shell 状态条 | 打开 Shell 会话窗口 | 同样有状态条（`[shell] ` 前缀被剥掉后，文字和颜色正确） | ☐ |
| E04 | 关窗无残留 | 关掉会话窗口 | 状态条跟着消失，没有残留 | ☐ |
| E05 | 校验提示保留 | IP / 端口 / 昵称为空时点 Connect；DNS 服务商没配置时 | 校验提示还在；「Ready: …」和查重提示还在 | ☐ |
| E06 | 语言四件套 | Settings → Appearance → Language 三选一（System / 中文 / English）；切换后即时生效 → 保存 → 重启 | 默认 System；切换即时生效；重启保持；旧配置没有这个字段也不会加载失败 | ☐ |
| E07 | 跟随系统 | Windows 不设环境变量，重启（中文系统 / 英文系统各试一次） | 系统是中文 → 中文界面；系统是英文 → 英文界面 | ☐ |
| E08 | 双向抽样 | 切到 English，随机看 20 处文字；切到中文，再看 20 处 | 没有中文 / 英文残留（KirinDesk / FPS / IP / TCP / PTY 这类专有名词除外） | ☐ |
| E09 | 缺键不崩溃 | 故意走一些少见路径 | 缺翻译时回退到中文、再缺就显示原键名，**绝不闪退** | ☐ |
| E10 | 命令行不受影响 | 跑 `kirin_desk --cli` 的各种子命令 | 命令行的提示语保持现状（CLI 没有做翻译） | ☐ |

---

## F 组 · 音频三个开关

> 测试会话中的三个音频开关。需要能出声的环境。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| F01 | ①允许麦克风（被控端） | Dashboard 关① → 再开 | 关 → 被控端停止发音频（主控端没声音）；开 → 恢复，**不用重连** | ☐ |
| F02 | ②客户端播放开关 | 连接窗口里关② / 开② | 立即静音 / 恢复 | ☐ |
| F03 | ③麦克风回传 | 连接窗口里开③ | 被控端能听到主控端的麦克风 | ☐ |
| F04 | 退出干净 | 音频会话中断开连接 | 播放线程干净退出，不卡住 | ☐ |
| F05 | 切换无爆音 | 会话中切换各个音频开关 | 没有「啪」的爆音 | ☐ |

---

## G 组 · GPU 硬件加速（需要特定显卡）

> ⚠️ 这一组**开发者的本机跑不了**（Intel 核显的 qsv 驱动损坏、NVIDIA 驱动版本不够），特别需要网友帮忙。需要 Intel 核显（qsv）或 NVIDIA 独显（nvenc）。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| G01 | 虚拟屏过滤 | 先激活一个虚拟显示器（比如某些远控 / 虚拟显示驱动），再开始捕获 | 虚拟屏会从捕获列表里滤掉、显示器索引不会错位 | ☐ |
| G02 | GPU 路径冒烟 | 分别设环境变量 `KIRIN_GPU_PREFER=intel` / `=nvidia` / 不设（回退） | intel → qsv 出码流；nvidia → nvenc 出码流；不设 → 默认可用。如果方便，4K 画面跑一轮看看 | ☐ |

> 💡 反馈时请附上你的**显卡型号和驱动版本**。

---

## H 组 · 界面美化

> 测试主题、配色、字号等视觉。单机可测，需要肉眼走查。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| H01 | 主题与字号 | 切换明亮 / 深色主题；核对字号 | 两个主题都能切；字号没有乱（标题 20 / 正文 18 / 大号 26 像素） | ☐ |
| H02 | 组件外观 | 走查卡片 / 导航 / 状态栏 / 按钮 / 输入框 / 徽标 | 组件齐全、状态颜色正确 | ☐ |
| H03 | 六个页面改造 | 走查 Dashboard / Devices / Connect / Settings / 会话弹窗 / 域名页 | 没有默认 egui 样式残留 | ☐ |
| H04 | 对比度与帧率 | 检查配色对比；观察帧率 | 文字和背景对比度够（不灰暗难读）；60 帧不回退、没有一直闪的动画 | ☐ |
| H05 | 深色主题对齐 | 对照深色模式各处 | 深色变量已对齐 | ☐ |

---

## I 组 · Tunnel 内网穿透

> 测试反向代理功能。可以单机（Server + Client 同机），也可以双机。

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| I01 | 独立页与保存 | 导航点 🚇 Tunnel；确认 Settings 页**没有** Tunnel 组；Tunnel 页点「保存」→ 查配置文件 → 重启 | 独立页正常；Settings 保存不再写 tunnel 字段；保存的字段正确、重启保持 | ☐ |
| I02 | 模式切换 | Client ↔ Server 模式切换 | 切换正常；Server 显示监听地址（默认 `0.0.0.0,::`）/ 端口（默认 7000）/ 端口范围（默认 `60000-61000`）；Client 显示代理列表 | ☐ |
| I03 | 多地址校验 | 监听地址填 `0.0.0.0,::,192.168.1.10`（混填，应能保存）；再分别填非 IP / 空段 / 域名 | 混填能保存；非法值有红边提示且**禁用保存** | ☐ |
| I04 | 多监听实测 | GUI 启动 Server，看日志；用 IPv4 和 IPv6 客户端各登录一次；再把 7000 端口占了启动 | 日志显示 `0.0.0.0:端口` 和 `[::]:端口` 双监听；IPv4 / IPv6 客户端都能登录；任一地址绑定失败 → 整体启动失败并显示原因 | ☐ |
| I05 | 缺省回归 | 删掉 `bind_addrs` 字段后启动；跑 `kirin_desk --cli self-test` | 回到旧的默认双栈行为；self-test 通过、只绑 `127.0.0.1` | ☐ |
| I06 | Token 生成与复制 | Server 区块点 ✏️ → 查配置文件；点 📋 → 粘贴验证；清空 Token 看 📋；切到 Client 区块 | ✏️ 生成 64 位十六进制 Token 并立即存盘（再点会覆盖）；📋 剪贴板内容 == 当前 Token；空 Token 时 📋 禁用；**Client 区块没有 ✏️📋** | ☐ |
| I07 | 发布个人网站 | 本机起一个 HTTP 服务；GUI 启动 Server（token + `0.0.0.0,::`）→ GUI 启动 Client（`web|127.0.0.1:80|8080`）→ 浏览器访问 | 定位文案是「通用 TCP 反向代理」（带明文流量提示）；`http://127.0.0.1:8080` 返回本地页面（IPv4 链路；IPv6 也试一下） | ☐ |
| I08 | 启停与失败态 | 停止 → 看按钮；分别用端口占用 / 空 token / 非法监听地址启动 | 停止后按钮变「▶ 启动」；失败 → 状态行「启动失败: <原因>（配置保持启用，下次启动将自动重试）」、按钮回「▶ 启动」、不崩溃 | ☐ |
| I09 | 状态持久化 | 启动 → 退出 → 重启；停止 → 退出 → 重启；恢复失败（端口被占） | 重启后自动恢复运行；停止后重启不自动运行；自动恢复失败会显示原因，下次会重试 | ☐ |
| I10 | 命令行兼容 | `tunnel start / serve / status`；GUI 启停前后查 `enabled` 字段 | 命令行行为和参数兼容；GUI 启停**不写** `enabled`（命令行设备注册不受影响） | ☐ |

---

## J 组 · 平台延伸（Linux / macOS，特别需要！）

> 🚨 **这两个平台的代码已经写好，但几乎没有真实设备验证过。** 如果你用 Linux 或 macOS，这组对你的反馈**非常非常需要**。不阻塞 Windows 发布，但对项目跨平台至关重要。

| # | 测试项 | 平台 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|------|
| J01 | 中文 + fcitx 输入法 | Linux | Shell 里 `echo 中文`；用 fcitx 输入法组合回显 | 中文和输入法组合期都正常 | ☐ |
| J02 | 中文 + macOS 拼音 | macOS | 同上，macOS 侧拼音输入法 | 中文和组合期正常 | ☐ |
| J03 | macOS 多显示器偏移 | macOS | 接双屏，副屏分别在**左 / 上 / 右 / 下**四个方向各试一次；同分辨率双屏；Retina 屏 | 鼠标注入落点正确、命中预期的屏幕、Retina 缩放换算正确 | ☐ |
| J04 | Linux 音频 | Linux | 测试音频采集和播放 | 音频链路正常（已改用 PipeWire） | ☐ |
| J05 | Linux 全链路 | Linux | 捕获 / 编码 / 解码 / 窗口 / 输入 各跑一轮 | 全部正常 | ☐ |

> 💡 Linux / macOS 反馈时，请附上**发行版 / 系统版本、桌面环境（GNOME / KDE / Wayland / X11）、PipeWire 版本**等信息。

---

## 📋 反馈模板（复制到 Issue 用）

````markdown
### 测试环境
- 主控端系统：（如 Windows 11 23H2）
- 被控端系统：（如 Windows 10 / Ubuntu 22.04 / macOS 14）
- 显卡：（如 Intel UHD 770 / NVIDIA RTX 3060 / 核显）
- 网络：（如同局域网 / 公网 IPv4 / IPv6）
- KirinDesk 版本：（git commit 或 release 版本号）

### 测试结果

#### A 组 · 远程桌面
- [x] A01 远程画面 — 正常，1080p 流畅，延迟约 30ms
- [ ] A02 双向音频 — ❌ 主控端听不到被控端声音，被控端日志报 xxx
- [ ] A03 键鼠控制 — ...

（按组列出你测过的项目）

### 发现的问题
（如果有 ❌，详细描述：现象、复现步骤、错误日志、截图）
````
