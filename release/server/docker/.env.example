# 复制为 .env 后填写（docker compose 会自动读取）：
#   cp .env.example .env
#   .env 不应提交到版本库（含敏感 token）

# 客户端认证 token（高熵 ≥32 字节；与客户端 [tunnel] token 一致）
# 生成示例：openssl rand -hex 32
KIRIN_RELAY_TOKEN=CHANGE_ME_high_entropy_token_32+bytes

# 日志级别：debug|info|warn|error
RUST_LOG=info
