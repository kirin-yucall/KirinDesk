# Rust / Cargo 构建产物
/target/

# 前端 node 依赖与构建产物(由 package.json + package-lock.json 还原)
ui/frontend/node_modules/
ui/frontend/dist/

# FFmpeg 二进制发行包(git 忽略;还原步骤见 Readme「克隆后从源码构建」)
ffmpeg/ffmpeg-8.1.1-full_build-shared/
release/ffmpeg/

# 编译产物与发布临时文件
# 注意:release/KirinDesk.exe 是发布用的正式二进制,需要入库跟踪
release/*.exe.bak-*
release/bench_results*.json

# 日志与临时文件
*.log
*.tmp
*.bak

# 系统文件
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# S-28 (F-33): 发布二进制移出版控 —— 发布走 CI artifacts（GitHub Releases + checksums.txt），本地保留文件供 install.bat 使用
release/KirinDesk.exe
# 本地发布暂存目录（zip/exe/deb/dmg/侧车/checksums，由 publish_local.sh 与 CI 产物汇聚）
release/dist/
